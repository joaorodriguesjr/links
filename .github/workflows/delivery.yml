name: Delivery workflow

on:
  push:
    branches:
    - prototype

jobs:
  docker-build:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3

    - name: Login
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: joaorodriguesjr
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build
      uses: docker/build-push-action@v2
      with:
        file: docker/frontend.dockerfile
        context: .
        tags: ghcr.io/joaorodriguesjr/links:${{ github.sha }}
        push: true

    - name: Before Secret
      run: |
        microk8s.kubectl delete secret links-registry --ignore-not-found

    - name: Secret
      run: |
        microk8s.kubectl create secret docker-registry links-registry \
        --docker-server=ghcr.io \
        --docker-username=joaorodriguesjr \
        --docker-password=${{ secrets.GITHUB_TOKEN }} \

    - name: Apply
      run: |
        sed 's#CONTAINER_IMAGE#ghcr.io/joaorodriguesjr/links:${{ github.sha }}#' kubernetes/deployment.yml | microk8s.kubectl apply -f -
        microk8s.kubectl apply -f kubernetes/service.yml
        microk8s.kubectl apply -f kubernetes/ingress.yml

    - name: Rollout
      run: |
        microk8s.kubectl rollout restart deployment.apps/links
